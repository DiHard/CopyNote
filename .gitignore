# Go
/bin/
/dist/
*.exe
*.test
*.out
vendor/

# Node / Vite
node_modules/
web/.vite/
*.log
# NOTE: web/dist/ IS committed — it's embedded into the Go binary via //go:embed.
# This lets `go build` work without requiring a Node toolchain at build time.

# IDE / OS
.vscode/
.idea/
.claude/settings.local.json
.DS_Store
Thumbs.db

# Build artifacts
*.syso
/build/

# Release signing (private key lives outside the repo; .sig is a release asset)
*.sig
*.key

# Sample snippet sets used for screenshots and manual testing, not shipped
/demo-data*.json
